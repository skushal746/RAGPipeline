FROM python:3.11-slim

WORKDIR /app

# Phase 2 — Python ML Worker (not yet implemented).
# Will consume DocumentIngestionEvents from Kafka, retrieve PDFs from S3,
# chunk text with LangChain, and generate vector embeddings via PyTorch/HuggingFace.

CMD ["echo", "ML Worker — Phase 2 not yet implemented."]
